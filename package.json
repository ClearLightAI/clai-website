{
  "name": "clai-website",
  "version": "1.0.0",
  "private": true,
  "description": "ClearLight AI marketing website",
  "scripts": {
    "dev": "npx serve . -l 3333 --no-clipboard",
    "preview": "npx serve . -l 3333 --no-clipboard"
  }
}
